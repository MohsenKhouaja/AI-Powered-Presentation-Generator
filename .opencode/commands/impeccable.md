---
description: Design, critique, polish, and refine frontend interfaces
---

The user invoked `/impeccable $ARGUMENTS`. This is a frontend design skill.

**First**, read and follow the instructions in `.github/skills/impeccable/SKILL.md`. That file defines the setup steps, sub-commands, and design guidance.

**Then** apply its routing rules (section "Routing rules") based on the user's argument.

All supporting files (scripts, references) are at `.github/skills/impeccable/`.
